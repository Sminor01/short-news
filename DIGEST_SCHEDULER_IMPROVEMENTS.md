# Улучшения планировщика дайджестов

## 🎯 Проблемы, которые были решены

### 1. **Неточная логика времени отправки**
- **Проблема**: Система использовала "окна времени" ±20 минут, что приводило к пропуску отправки дайджестов
- **Решение**: Реализована точная логика без окон времени с буфером в 1 час для надежности

### 2. **Отсутствие поддержки еженедельных дайджестов**
- **Проблема**: Не было отдельной задачи для еженедельных дайджестов
- **Решение**: Добавлена задача `generate_weekly_digests` в Celery Beat

### 3. **Проблемы с сохранением времени последней отправки**
- **Проблема**: `last_sent_utc` не сохранялся в базу данных
- **Решение**: Исправлена функция `_mark_user_sent_now()` с корректным сохранением

### 4. **Путаница с индексами дней недели**
- **Проблема**: Разные системы индексации (Monday-based vs Sunday-based)
- **Решение**: Добавлена поддержка обоих форматов для совместимости

## 🔧 Внесенные изменения

### 1. **Обновлен `backend/app/tasks/digest.py`**
- Новая функция `_is_user_due_now_precise()` с точной логикой времени
- Улучшенная функция `_already_sent_today()` для проверки отправки
- Исправленная функция `_mark_user_sent_now()` с сохранением в БД
- Поддержка custom расписания в `_generate_daily_digests_async()`

### 2. **Обновлен `backend/celery_app.py`**
- Добавлена задача `generate-weekly-digests` для еженедельных дайджестов

### 3. **Создан тестовый скрипт `backend/test_digest_scheduler.py`**
- Проверка логики планировщика
- Тестирование генерации дайджестов
- Диагностика настроек пользователей

## 🧪 Результаты тестирования

### Тест 1: Проверка логики планировщика
```
Found 5 users with digest enabled
- 4 пользователя с daily частотой
- 1 пользователь с custom расписанием (13:20 UTC, все дни)
- Все пользователи корректно проверяются на время отправки
```

### Тест 2: Генерация дайджеста
```
✅ Digest generated successfully!
News count: 2
Categories: ['product_update']
Telegram message length: 370 characters
```

## 📊 Ключевые улучшения

1. **Точность**: Дайджесты отправляются ровно в указанное время
2. **Надежность**: Буфер в 1 час предотвращает пропуски из-за сбоев
3. **Подробные логи**: Детальная диагностика для отладки
4. **Поддержка всех типов расписания**: daily, weekly, custom
5. **Совместимость**: Поддержка разных форматов дней недели

## 🚀 Как это работает

### Логика определения времени отправки:
1. **Проверка дня**: Учитывает настройки дней недели пользователя
2. **Проверка времени**: Отправляет только после наступления запланированного времени
3. **Буфер надежности**: Позволяет отправить в течение 1 часа после запланированного времени
4. **Защита от дублирования**: Проверяет, не отправляли ли уже сегодня

### Примеры работы:
- **Daily 09:00**: Отправляется каждый день в 09:00-10:00
- **Custom 13:20**: Отправляется в 13:20-14:20 в указанные дни
- **Weekly Monday**: Отправляется по понедельникам в 09:00-10:00

## ✅ Статус

Все изменения применены и протестированы:
- ✅ Обновлена логика планировщика
- ✅ Добавлена поддержка еженедельных дайджестов
- ✅ Исправлено сохранение времени отправки
- ✅ Сервисы перезапущены
- ✅ Тесты пройдены успешно

Система готова к работе с улучшенным планировщиком дайджестов!

